{% set name = "flet" %}
{% set version = "0.6.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/flet-{{ version }}.tar.gz
  sha256: 1033404f03b63788d7542e47d55997a82f571939d0a3cbcbbd3128cd1619ba3e

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  entry_points:
    - flet = flet.cli.cli:main

requirements:
  host:
    - pip
    - poetry-core
    - python >=3.7
  run:
    - flet-core ==0.5.2
    - httpx >=0.23.3,<0.24.0
    - oauthlib >=3.2.2,<4.0.0
    - packaging >=23.0,<24.0
    - python >=3.7
    - typing_extensions >=4.4.0,<5.0.0
    - watchdog >=2.2.1,<3.0.0
    - websockets >=10.4,<11.0
    - websocket-client >=1.4.2,<2.0.0

test:
  imports:
    - flet
  commands:
    - pip check
    - flet --help
  requires:
    - pip

about:
  home: https://github.com/flet-dev/flet
  dev_url: https://github.com/flet-dev/flet/tree/main/sdk/python
  doc_url: https://flet.dev/docs/
  summary: Flet for Python - easily build interactive multi-platform apps in Python
  license: Apache-2.0
  license_file:
    - LICENSE

extra:
  recipe-maintainers:
    - thewchan
